DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

CODEF_MODE=mock
CODEF_CLIENT_ID=
CODEF_CLIENT_SECRET=
CODEF_API_BASE_URL=
CODEF_TIMEOUT_SECONDS=20
PAYMENT_GATEWAY_MODE=mock
CODEF_PUBLIC_KEY=

# 운영/공유 환경에서는 `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`로 생성한 값을 사용한다.
APP_ENCRYPTION_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
OPENAI_TIMEOUT_SECONDS=20
OPENAI_MAX_OUTPUT_TOKENS=1200
OPENAI_REASONING_EFFORT=none
# 비워두면 API 키가 있는 로컬/운영에서는 OpenAI, 테스트에서는 규칙 기반 응답기를 사용한다.
CHAT_RESPONDER_CLASS=
